public/
resources/
.hvm
.hugo_build.lock

# macOS
.DS_Store

# Local Hugo overrides (e.g. baseURL for localhost); not tracked
hugo.local.toml

# Local reference copy of ../comet (wireframes + docs); not tracked
comet/

# Agent/dev notes kept outside the repo
AGENTS.md
